def reverseArray(arr, m):
    # Write your code here.
    n = len(arr)
    start = m + 1
    end = n - 1
    while start < end:
        arr[start], arr[end] = arr[end], arr[start]
        start += 1
        end -= 1
    return arr
